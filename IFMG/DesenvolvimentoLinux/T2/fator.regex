#Variavel
(
    ( )*
    (^[a-zA-Z_]+([0-9]*[a-zA-Z_]*)*)
    ( )*
)
#Igual
(=)
#Array
(
    #Init
    ( )*
    \(
    ( )*
    #Digit
    ( )*
    ([0-9]+(\.[0-9]+)?)
    ( )*
    #Se tiver mais de 1 digito
    ( )*
    (
        #Virgula
        (
            ( )*
            (\,)
            ( )*
        )
        #Digito
        (
            ( )*
            ([0-9]+(\.[0-9]+)?)
            ( )*
        )
    )*
    ( )*
    #End
    \)
)